const EndGame = () => {

    return (
        <div id="leaderboards-container">
            <div id="leaderboard-easy">easy leaderboard</div>
            <div id="leaderboard-medium">medium leaderboard</div>
            <div id="leaderboard-hard">hard leaderboard</div>
            <button>Home</button>
        </div>
    );
}

export default EndGame;